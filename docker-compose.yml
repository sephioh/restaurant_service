version: '3'
services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    working_dir: /app/restaurant_service
    command: bash -c "pipenv run python manage.py migrate && pipenv run python manage.py runserver 0:8000"
